export default class Picture {
    constructor(src, x, y, lenght, height) {
        this.src = src;
        this.x = x;
        this.y = y;
        this.lenght = lenght;
        this.height = height;
    }
}